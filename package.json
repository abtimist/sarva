{
  "name": "sarva",
  "version": "3.0.0",
  "description": "Real-time multilingual classroom captioning",
  "private": true,
  "scripts": {
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev",
    "install:all": "cd server && npm install && cd ../services/python && pip install -r requirements.txt",
    "setup:data": "mkdir -p data"
  },
  "license": "MIT"
}
